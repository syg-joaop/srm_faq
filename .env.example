# Database
DATABASE_URL=postgresql://srm_user:srm_secret_2024@localhost:5432/srm_faq

# Ollama (para embeddings locais)
OLLAMA_URL=http://localhost:11434

# GROQ API (para humanizar respostas)
GROQ_API_KEY=your_groq_api_key_here

# Humanizacao de respostas
# true  = Usa GROQ para gerar respostas mais naturais (mais lento, requer API key)
# false = Retorna resposta direta do banco via embedding match (mais rapido)
# Pode ser sobrescrito por request via parametro `humanize` no chat
USE_GROQ_HUMANIZE=true
